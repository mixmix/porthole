{
  "name": "porthole",
  "version": "0.0.1",
  "description": "an http windows into ssb",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mixmix",
  "license": "ISC",
  "dependencies": {
    "pull-stream": "^2.28.4",
    "routes": "^2.1.0",
    "ssb-client": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.8.1"
  }
}
